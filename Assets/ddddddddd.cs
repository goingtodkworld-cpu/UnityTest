using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ddddddddd : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("°Å³ª");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}