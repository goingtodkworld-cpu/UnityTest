using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class sdfsdsdf : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("fdfds"); 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
